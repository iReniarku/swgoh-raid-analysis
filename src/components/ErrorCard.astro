---
export interface Props {
  title?: string;
  message: string;
  icon?: string;
}

const { title = "Error", message, icon = "⚠️" } = Astro.props;
---

<div class="glass-card border-red-500/30 bg-red-500/10 p-6 max-w-2xl mx-auto">
  <div class="flex items-start gap-4">
    <div class="flex-shrink-0">
      <div class="w-12 h-12 rounded-full bg-red-500/20 flex items-center justify-center">
        <span class="text-red-400 text-xl">{icon}</span>
      </div>
    </div>
    
    <div class="flex-1">
      <h3 class="text-red-400 text-lg font-bold mb-2">{title}</h3>
      <p class="text-red-200 mb-4">{message}</p>
      
      <div class="flex gap-3">
        <button 
          onclick="location.reload()" 
          class="px-4 py-2 bg-red-500/20 hover:bg-red-500/30 text-red-300 border border-red-500/30 rounded-lg transition-colors"
        >
          🔄 Retry
        </button>
        
        <a 
          href="/help/" 
          class="px-4 py-2 bg-cyan-500/20 hover:bg-cyan-500/30 text-cyan-300 border border-cyan-500/30 rounded-lg transition-colors"
        >
          ❓ Get Help
        </a>
      </div>
    </div>
  </div>
  
  <div class="mt-4 pt-4 border-t border-red-500/20">
    <p class="text-gray-400 text-sm">
      If this problem persists, please check that your CSV files are in the correct format and location.
    </p>
  </div>
</div>
