---
import Logo from './Logo.astro';
import Navigation from './Navigation.astro';
import { PROJECT_CONFIG } from '../config/project.ts';
import '../styles/global.css';

export interface Props {
  frontmatter: {
    title: string;
  };
}

const { frontmatter } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href={`${PROJECT_CONFIG.baseUrl}favicon.svg`} />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{frontmatter.title}</title>
  </head>
  
  <body class="p-2 sm:p-5">
    <div class="max-w-7xl mx-auto mobile-content">
      <div class="flex flex-col items-center mb-4 sm:mb-6">
        <div class="flex items-center gap-3 mb-2 sm:mb-4">
          <Logo size="lg" className="mobile-logo" />
          <h1 class="text-center text-heading text-2xl sm:text-4xl mobile-title">{PROJECT_CONFIG.title}</h1>
        </div>
        <p class="text-center text-gray-400 text-xs sm:text-sm mt-1 sm:mt-2">{PROJECT_CONFIG.description}</p>
      </div>
      
      <Navigation />
      
      <div class="mdx-content max-w-4xl mx-auto glass-card p-3 sm:p-6 mt-6 sm:mt-8">
        <slot />
      </div>

      <script define:vars={{ PROJECT_CONFIG }}>
        window.PROJECT_CONFIG = PROJECT_CONFIG;
      </script>

      <div class="text-center mt-8 sm:mt-12 py-4 sm:py-6 border-t border-gray-700">
        <div class="flex justify-center mb-2 sm:mb-3">
          <Logo size="sm" />
        </div>
        <p class="text-gray-400 text-xs sm:text-sm">{PROJECT_CONFIG.title} for Star Wars: Galaxy of Heroes</p>
        <p class="text-gray-500 text-xs mt-1">
          Built with Astro.js • Automatically updated based on CSV files from Wookiebot
          <br />
          <a href={PROJECT_CONFIG.githubUrl} target="_blank" rel="noopener noreferrer" class="text-cyan-400 hover:text-cyan-300 transition-colors">
            📂 View on GitHub
          </a>
        </p>
      </div>
    </div>
  </body>
</html>
